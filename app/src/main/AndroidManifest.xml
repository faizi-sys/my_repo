<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iiui_project">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET " />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />


    <uses-permission android:name="android.permission.READ_SMS" />
    <!--

         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET " />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
<activity android:name=".DriverDashoard.ViewDriverActivityToSeeUserDerive"/>
        <activity android:name=".USerDashboard.UpdateLocationActvityUser"/>
        <activity
            android:name=".USerDashboard.EmergencyAleryMEssage"
            android:label="@string/title_activity_emergency_alery_message"></activity>
<activity android:name=".DriverDashoard.ViewUserActivity"/>
        <activity android:name=".DriverActivities.Add_Personal_Derive" />
        <activity android:name=".MapPackage.OnlyLocationViewBoth"/>
        <activity android:name=".See_Notification" />
        <activity android:name=".See_Notification_Accept_User" />
        <activity android:name=".BiddingRelatedCommentActivity" />
        <activity android:name=".DriverDashoard.DriverUpdateActivity" />
        <activity android:name=".DriverDashoard.UserUpdateActivityDriver" />
        <activity android:name=".See_Notification_Accept_Driver" />
<activity android:name=".USerDashboard.ViewDerivesByUser"/>
        <activity
            android:name=".MapPackage.OnlyLocationView"
            android:label="@string/title_activity_only_location_view" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".USerDashboard.ViewDerivesByUser" />
    <activity android:name=".USerDashboard.Add_Personal_Derive"/>
        <activity
            android:name=".MapPackage.ViewDeriveLocation"
            android:label="@string/title_activity_view_derive_location" />
        <activity android:name=".DriverActivities.ViewDerives" />
        <activity android:name=".DriverActivities.AddDerives" />
        <activity android:name=".DriverDashoard.DrivereDashboard" />
        <activity android:name=".USerDashboard.UpdateLocationActvity" />
        <activity android:name=".USerDashboard.UserUpdateActivityUser" />
        <activity android:name=".MapPackage.MapsActivity" />
        <activity android:name=".USerDashboard.ViewDriverActivity" />
        <activity
            android:name=".MapPackage.SetMapHouse"
            android:label="@string/title_activity_set_map_house" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPackage.LoginActivity"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".LoginPackage.RegistrationActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".USerDashboard.UserDashboard" />
    </application>

</manifest>